#!/usr/bin/env raku

sub default {
    run <zef install --force .>
}

if not @*ARGS {
    @*ARGS = ['default'];
}

for @*ARGS -> $arg {
    if not &::($arg):exists {
        note "No such task: $arg";
        exit 1;
    }

    &::($arg)();
}
